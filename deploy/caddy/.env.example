# Copy to .env on the server. One user/password-hash pair per protected app.
# Generate a hash with:
#   docker run --rm caddy:2 caddy hash-password --plaintext 'the-password'
# Keep the single quotes around the hash: it contains $ signs that Docker
# Compose would otherwise try to expand.

MELBOURNE_MOODS_USER=workshop
MELBOURNE_MOODS_PASSWORD_HASH='$2a$14$replace-me-with-output-of-caddy-hash-password'

GRAINGER_USER=workshop
GRAINGER_PASSWORD_HASH='$2a$14$replace-me-with-output-of-caddy-hash-password'

# Reuses the same shared login as the other Streamlit apps: paste the same hash.
PARADISEC_AUDIO_USER=workshop
PARADISEC_AUDIO_PASSWORD_HASH='$2a$14$replace-me-with-output-of-caddy-hash-password'
