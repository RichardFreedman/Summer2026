# Copy to .env on the server. .env is gitignored.
# (The shared login lives in deploy/caddy/.env, not here.)
#
# The app has no key entry box: participants use this key, which is why the
# app sits behind basic auth.

OPENAI_API_KEY=sk-...
