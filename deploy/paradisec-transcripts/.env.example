# Copy to .env on the server. .env is gitignored.
# (The shared login lives in deploy/caddy/.env, not here.)
#
# Used for embeddings (text-embedding-3-large) and answers (gpt-4o-mini).
# The workshop's existing key from deploy/grainger/.env is fine here.
OPENAI_API_KEY=sk-...

# Optional overrides
# RAG_CHAT_MODEL=gpt-4o-mini
# RAG_EMBEDDING_MODEL=text-embedding-3-large
