Extracted_texts contains data OCR'd from the Melbourne Rare Books Concert Collection via the multipdf_extraction.py program (see Multipage_PDF_OCR). Structured_data contains a JSON file of data extracted from the text files, as well as an errors log, both generated by Claude.  
